# GOOGLE_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxx
# 1. Set the AI Provider to use the OpenAI-compatible service, should be 'openai' or 'google' 
AI_PROVIDER=openai

# 2. Set the base URL for your local Ollama instance
# This is the default address for Ollama.
OPENAI_API_BASE_URL=http://localhost:11434/v1

# 3. Set the API key (for local Ollama, this can be any non-empty string)
OPENAI_API_KEY=ollama

# 4. Set the exact name of your local model
OPENAI_MODEL_NAME=gemma3:12b